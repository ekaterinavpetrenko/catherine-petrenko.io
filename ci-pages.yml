name: CI & Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pages: write         # нужно для деплоя Pages
  id-token: write      # нужно для деплоя Pages

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  test:
    name: Run Jest
    runs-on: ubuntu-latest

    steps:
      # 🧾 Получаем код
      - name: Checkout
        uses: actions/checkout@v4

      # 🧰 Ставим Node.js (можно поменять версию при необходимости)
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # 📦 Ставим зависимости
      - name: Install deps
        run: npm ci

      # 🧪 Запускаем тесты
      - name: Run tests
        run: npm test -- --ci

  deploy:
    name: Deploy to GitHub Pages
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ➕ (опционально) если у тебя есть сборка (webpack/vite), вставь шаг сборки здесь

      # 📦 Подготавливаем артефакт сайта
      # Если сайт лежит в корне репо (index.html, styles.css, script.js),
      # то можно передать path: '.' как есть.
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      # 🚀 Публикуем
      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
